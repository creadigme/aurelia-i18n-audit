/**
 * Diffic... Audit level
 */
export enum ELevel {
  /** MEDIUM */
  DEFAULT = 0,
  /** Just have all languages ok */
  EASY = 1,
  /** No missing keys & all languages ok (default) */
  MEDIUM = 2,
  /** No missing keys && no unused keys & all languages ok */
  HARD = 3,
}
